import React from 'react'
import { Link, NavLink } from 'react-router-dom';

function MyNavbar() {
    return (
        <nav>
            <NavLink to="/">Home</NavLink> | <Link

        </nav>
    );
}

export default MyNavbar
